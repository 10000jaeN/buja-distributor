# 작업로그

---

## 2026-06-26

### 어드민 로그인 pending 상태 처리

**내용**
- 로그인 중 이메일·비밀번호 인풋 `disabled` 처리
- 로그인 버튼에 `Loader2` 스피너 아이콘 추가

**변경 파일**
- `frontend/src/app/admin/login/page.tsx`

---

### 상품 Q&A 작성 기능 추가 (비밀글 지원)

**내용**
- Q&A 목록 조회, 작성, 삭제 기능 구현
- 비밀글 작성 가능 (작성자·어드민만 내용 열람, 나머지는 "비밀글입니다." 마스킹)
- 판매자 답변 표시
- 로그인하지 않은 경우 작성 버튼 미노출, 안내 문구 표시

**변경 파일**
- `frontend/src/types/qna.ts` — 신규: Qna, CreateQnaInput 타입
- `frontend/src/api/qnaService.ts` — 신규: 목록 조회·작성·삭제 API
- `frontend/src/app/(main)/products/[slug]/_components/QnAWriteDialog.tsx` — 신규: 문의 작성 다이얼로그
- `frontend/src/app/(main)/products/[slug]/_components/ProductQnATab.tsx` — Q&A 목록/작성/삭제로 전면 교체
- `frontend/src/app/(main)/products/[slug]/_components/ProductTabs.tsx` — `productId`, `productName` prop 추가
- `frontend/src/app/(main)/products/[slug]/page.tsx` — ProductTabs에 prop 전달

---

### 어드민 회원 관리 페이지 추가

**내용**
- 회원 목록 조회 (닉네임/이메일 검색, 가입경로·역할 표시)
- 회원 삭제 (어드민 계정 삭제 방지)
- 데스크탑 테이블 / 모바일 카드 반응형
- 어드민 사이드바에 "회원 관리" 메뉴 추가

**변경 파일**
- `frontend/src/api/adminUserService.ts` — 신규: 회원 목록 조회·삭제 API
- `frontend/src/app/admin/users/page.tsx` — 신규
- `frontend/src/app/admin/users/_components/AdminUsersClient.tsx` — 신규
- `frontend/src/app/admin/AdminShell.tsx` — 사이드바 메뉴 추가

---

### Q&A 백엔드 API 추가

**내용**
- Q&A 모델, 컨트롤러, 라우터 생성 및 server.js 등록
- 비밀글 열람 제어: 선택적 인증(optionalAuthMiddleware)으로 본인·어드민만 내용 반환
- 어드민 전용 전체 목록 조회 (answered 필터), 미답변 카운트 API

**변경 파일**
- `backend/src/api/qna/qna.model.js` — 신규
- `backend/src/api/qna/qna.controller.js` — 신규
- `backend/src/api/qna/qna.routes.js` — 신규
- `backend/src/middleware/optionalAuth.middleware.js` — 신규
- `backend/server.js` — qnaRouter 등록

---

### 어드민 회원 관리 API 개선 + 엔드포인트 경로 수정

**내용**
- `getAllUsers`에 닉네임/이메일 검색(`?search=`) 지원 추가
- 빈 결과 시 404 → 빈 배열 반환으로 수정
- 어드민 회원 삭제 `DELETE /user/admin/delete/:userId` 추가 (어드민 계정 삭제 방지)
- 프론트 엔드포인트 경로 `/admin/users` → `/user/admin/all` 수정

**변경 파일**
- `backend/src/api/user/user.controller.js`
- `backend/src/api/user/user.routes.js`
- `frontend/src/api/adminUserService.ts`

---

### 어드민 Q&A 관리 페이지 및 미답변 뱃지 추가

**내용**
- 어드민 Q&A 관리 페이지: 전체·미답변·답변완료 필터, 답변 등록·수정·삭제
- `QnAAnswerDialog`: 원본 문의 + 답변 입력 폼 (등록·수정 통합)
- 사이드바 "Q&A 관리" 메뉴 추가
- 미답변 Q&A 수 빨간 뱃지 표시 (페이지 이동 시마다 갱신)

**변경 파일**
- `frontend/src/api/adminQnaService.ts` — 신규
- `frontend/src/app/admin/qna/page.tsx` — 신규
- `frontend/src/app/admin/qna/_components/AdminQnaClient.tsx` — 신규
- `frontend/src/app/admin/qna/_components/QnAAnswerDialog.tsx` — 신규
- `backend/src/api/qna/qna.controller.js` — 어드민 전체 목록·미답변 카운트 추가
- `backend/src/api/qna/qna.routes.js` — 어드민 라우트 추가
- `frontend/src/app/admin/AdminShell.tsx` — Q&A 메뉴·뱃지 추가
