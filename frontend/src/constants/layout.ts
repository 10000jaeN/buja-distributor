/** Nav 높이. app/layout.tsx에서 --nav-height CSS 변수로 주입됨 */
export const NAV_HEIGHT = 69;

/** 모바일 하단 구매 바 높이 (ProductActions). app/layout.tsx에서 --mobile-bottom-bar-height CSS 변수로 주입됨 */
export const MOBILE_BOTTOM_BAR_HEIGHT = 89;
