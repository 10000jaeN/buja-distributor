# 의논사항

---

## 1. 카테고리 삭제 시 상품 처리 정책

**현황**

- 소분류/대분류 삭제 시 해당 카테고리에 속한 상품은 삭제되지 않음
- 상품의 `category.parent` / `category.child` 필드값은 그대로 유지됨
- 카테고리가 Nav에서 사라질 뿐, 상품 데이터는 DB에 남아있음 (고아 상품 발생)

**선택지**

1. 현행 유지 — 카테고리 삭제해도 상품은 보존 o
2. 연동 삭제 — 카테고리 삭제 시 해당 상품도 함께 삭제
3. 카테고리 초기화 — 상품의 카테고리 필드를 비워두고 "미분류" 처리

---

## 2. 결제 플로우 미구현

**현황**

- 장바구니 "구매하기" 버튼 — UI만 있고 실제 결제 연동 없음
- 상품 상세 페이지 "구매하기" 버튼 — 동일하게 미연동
- Order 생성 API는 백엔드에 구현되어 있음 (`POST /orders`)

**필요 작업**

- PG사 연동 결정 (토스페이먼츠, 카카오페이 등)
- 배송지 입력/선택 UI
- 주문 완료 페이지

---

## 3. 미구현 페이지

**현황**

- `/best` (베스트 상품) — Nav 메뉴에 링크만 있고 페이지 없음
- `/free-shipping` (무료배송) — 동일
- `/signup` (회원가입) — 사이드바에 링크만 있고 페이지 없음

**선택지**

- 소셜 로그인만 쓸 경우 `/signup` 제거
- `/best`, `/free-shipping` 기획 확정 후 구현

---

## 4. S3 버킷 퍼블릭 액세스 설정

**현황**

- 현재 S3 버킷 퍼블릭 액세스 차단 설정을 모두 해제한 상태
- 이미지 URL이 외부에 노출되면 누구나 접근 가능

**선택지**

- 현행 유지 (테스트 단계라 무방)
- CloudFront + 서명된 URL로 접근 제한 (배포 전 검토 권장)

---

## 5. 도서산간 추가배송비

**현황**

- 현재 도서산간 지역에 대한 추가배송비 처리 없음
- 배송지 입력 UI 자체가 미구현 상태

**필요 작업**

- 제주/도서산간 지역 판별 로직 (우편번호 or 주소 기반)
- 추가배송비 금액 정책 결정 (고정 금액 vs 상품별 상이)
- 결제 플로우(#2) 및 배송지 입력 UI 구현과 함께 진행 필요

---

## 6. 장바구니 배송비 합산 정책

**현황**

- 현재 장바구니에서 배송비를 상품별로 각각 계산하여 합산
- 예: A상품 배송비 3,000원 + B상품 배송비 3,000원 = 총 배송비 6,000원

**선택지**

1. 현행 유지 — 상품별 배송비 각각 합산
2. 전체 합계 기준 — 선택된 상품 총 금액이 특정 기준 이상이면 전체 무료
3. 판매자 단위 묶음 — 같은 판매자 상품끼리 묶어서 배송비 적용 (판매자 개념 도입 시)

---

## 7. 사이드바 아코디언 추가

**현황**

- 카테고리 펼칠 시 display 변경

**개선안**

- 카테고리 펼칠 시 아코디언 애니메이션 추가

---

## 8. 주문 개별 송장 입력

**현황**

- 현재 주문별 개별 송장 입력 기능 미구현

**선택지**

1. 현행 유지 — 개별 송장 입력 불가, 일괄 처리로만 운영
2. 개별 송장 입력 구현 — 주문 단건별 송장번호 입력 UI 추가

---

## 9. 포인트 기능

**현황**

- 포인트 기능 미구현 — 할인/쿠폰 시스템과 함께 Phase별로 구현 예정 (항목 13 참고)

**확정 정책**

- 적립 시점: 구매 확정 시 (환불 문제 방지)
- 적립률: 기본 3%, 어드민 Settings에서 수정 가능
- 유효기간: 필드는 있되 현재 null (무제한)로 운영
- 최소 사용 단위: 없음 (1원 단위 사용 가능)
- 적용 순서: (프로모션 OR 쿠폰) → 포인트 차감
- 포인트 이력은 PointTransaction 별도 컬렉션으로 관리

---

## 10. 서버 컴포넌트 전환 및 아키텍처 개선

**현황**

- 현재 모든 페이지 컴포넌트가 `"use client"` — 데이터 페칭·상태관리·UI 렌더링이 한 파일에 혼재
- apiClient가 서버 환경에서 토큰 주입 불가 (localStorage 접근 불가)
- Zustand 스토어(useAuthStore 등)가 서버에서 사용 불가

**전환 시 기대 효과**

- 초기 로딩 속도 개선 — 서버에서 데이터 페칭 후 HTML로 내려와 `isLoading` 스피너 제거
- JS 번들 크기 감소 — 서버 컴포넌트는 번들에 미포함
- SEO 개선 — 상품 목록/상세 등 SSR 처리 가능
- 보안 강화 — 민감한 로직이 서버에만 존재

**필요 작업**

- 서버/클라이언트 apiClient 분리 (`serverApiClient.ts` — cookies()로 토큰 주입)
- 인증 정보를 Zustand 대신 쿠키 기반으로 이동
- 페이지 컴포넌트를 데이터 페칭(서버)과 인터랙션(클라이언트)으로 분리

**전환 우선순위 (안)**

1. 인터랙션이 적은 페이지부터 — 상품 목록, 상품 상세
2. 이후 마이페이지, 어드민 페이지 순차 전환

**주의**

- 기능 개발이 어느 정도 마무리된 후 진행 권장
- 아키텍처 변경이라 작업 범위가 크므로 별도 스프린트로 계획 필요

---

## 11. 커스텀 도메인 적용

**현황**

- 현재 Vercel 기본 도메인으로 배포 중
- 커스텀 도메인 적용 필요

**결정 사항**

- 도메인 등록업체: **Cloudflare Registrar** (원가 판매, 갱신 시 인상 없음)
- DNS 관리: **Cloudflare DNS**
- 배포 플랫폼: 현재 **Vercel**, 추후 **AWS 이전 계획 있음**

**Cloudflare를 선택한 이유**

- 배포 플랫폼이 바뀌어도 DNS 레코드만 수정하면 되어 플랫폼 종속 없음
- Vercel → AWS 이전 시 도메인/DNS 변경 불필요
- DDoS 보호, CDN 무료 제공

**연결 순서 (Vercel 기준)**

1. Cloudflare Registrar에서 도메인 구매
2. Vercel → Settings → Domains → 도메인 추가
3. Vercel이 안내하는 DNS 레코드를 Cloudflare DNS에 입력

**주의**

- Vercel에서 직접 도메인 구매 금지 — Vercel이 DNS 관리권을 가져 AWS 이전 시 번거로워짐

**도메인 이름 후보**

| 도메인 | 특징 |
|--------|------|
| `bujayutong.com` | 발음 그대로, 직관적 |
| `buja-store.com` | 짧고 기억하기 쉬움 |
| `bujamart.com` | 마트 느낌, 커머스 연상 |
| `bujashop.com` | 쇼핑몰 느낌 명확 |
| `부자유통.kr` | 한글 도메인, 모바일 입력 편리 |

- TLD는 `.com` (신뢰도) 또는 `.co.kr` (국내 서비스) 권장
- 구매 전 Cloudflare에서 등록 가능 여부 확인 필요

---

## 12. 배송비

- 제주 3000, 도서산간 5000
- 묶음배송 가능여부
- 반품/교환 배송비 -> 상품별로 기본 3000

---

## 13. 할인 / 쿠폰 / 포인트 시스템

**확정 정책**

- 프로모션 OR 쿠폰 중 하나만 적용 가능 (중복 불가)
- 적용 순서: (프로모션 OR 쿠폰) → 포인트 차감 → 최종 결제금액
- 포인트 정책은 항목 9 참고

**계산 구조**

```
상품 합계(subtotal)
    ↓
프로모션 할인 OR 쿠폰 할인 (둘 중 하나)
    ↓
포인트 차감
    ↓
배송비 합산
    ↓
최종 결제금액
```

**모델 설계**

- `Promotion`: 상품/카테고리/전체 대상, 기간·수량·최소주문금액 조건, percentage/fixed
- `Coupon`: 코드 기반, 1인당 사용 횟수 제한, 최소주문금액 조건, 유효기간
- `CouponUsage`: 쿠폰 사용 이력 (user + coupon + order)
- `PointTransaction`: 포인트 적립/차감/취소 이력
- `User`에 `points` 필드 추가
- `Order`에 `appliedPromotion`, `coupon`, `couponDiscount`, `pointsUsed`, `pointsEarned` 필드 추가
- `Settings`에 `pointRate` (기본 3%) 추가

**구현 계획 (Phase별 PR)**

| Phase | 내용 |
|-------|------|
| Phase 1 | 백엔드 모델·API — Promotion, Coupon, CouponUsage, PointTransaction, Order/User 업데이트 |
| Phase 2 | 어드민 UI — 프로모션 관리, 쿠폰 관리, 포인트 적립률 설정 |
| Phase 3 | 사용자 UI — 결제 페이지 쿠폰/포인트 적용, 마이페이지 포인트 내역 |

---

## 14. 회원 전화번호 관리

**✅ 완료 (2026-07-16)**

- User 스키마에 `phoneNumber` 필드 추가 (선택, match 정규식 검증)
- 마이페이지 프로필에서 인라인 편집 가능
- 어드민 회원 상세 모달 노출
